Quilt is in early stages of development.  You will need the modules:

python-pyro
python-daemon
python-argparse

The rest should be std python, but if you find something, add it above.

To run:

$ test/bin/start_test


Then after you are satisfied:

$ test/bin/stop_test
